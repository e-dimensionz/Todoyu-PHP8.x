<div id="project-infobox-presetlist" class="infoBox presets">
	<h4 id="project-infobox-presetlist-header">{Label text='project.ext.infobox.presets.title'}</h4>
	<div id="project-infobox-presetlist-content" class="content overflow">
		<table class="list presetsListing">
		{foreach $presets as $preset}
			<tr class="{cycle values="odd,even"}">
				<th>{Label text=$preset.label}</th>
				<td>{$preset.value}</td>
			</tr>
		{/foreach}
		</table>
	</div>
</div>